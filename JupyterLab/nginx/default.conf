# --------------------------------------------------------
# SERVER 1: TensorFlow Environment (Now on Port 8888)
# --------------------------------------------------------
server {
    listen 8888;
    listen [::]:8888;
    server_name art.local; 
    
    client_max_body_size 2G;
    error_log /dev/stderr debug;

    location / {
        # Proxy to the TENSORFLOW container
        proxy_pass http://jupyterlab-tensorflow:8888; 
        
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header Host $http_host;
        proxy_http_version 1.1;
        proxy_redirect off;
        proxy_buffering off;
        
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 86400;
    }
}

# --------------------------------------------------------
# SERVER 2: PyTorch Environment (Now on Port 8889)
# --------------------------------------------------------
server {
    listen 8889;
    listen [::]:8889;
    server_name art.local; 
    
    client_max_body_size 2G;
    error_log /dev/stderr debug;

    location / {
        # Proxy to the PYTORCH container
        proxy_pass http://jupyterlab-pytorch:8888; 
        
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header Host $http_host;
        proxy_http_version 1.1;
        proxy_redirect off;
        proxy_buffering off;
        
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 86400;
    }
}